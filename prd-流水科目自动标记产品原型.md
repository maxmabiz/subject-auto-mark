# PRD：流水科目自动标记产品原型

## 1. Summary

本需求面向资金和财务人员，建设一套“流水科目自动标记”后台产品。系统通过人工标记、飞书付款审批匹配、渠道流水规则识别三类来源确定流水科目，并按照“人工标记 ＞ 飞书审批匹配 ＞ 渠道规则识别”的固定优先级生成最终结果。

第一期需要完成可交互的高保真前端原型，重点展示流水科目标记、Excel规则导入、异常处理、人工修改、匹配依据和版本追溯。不建设可视化规则编辑器，不生成会计凭证。

## 2. Problem

- 当前财务人员需要在系统外通过Excel对资金流水逐笔判断并标记科目，再将完成标记的结果导入系统。
- 该流程重复操作多、耗时长，流水量增加后人力投入持续上升。
- 人工判断容易受到人员经验影响，存在漏标、错标和口径不一致的问题。
- 业务方已经形成基于“平台、账号、检索字段、检索关键词”的Excel规则，但这些规则目前仍依赖人工执行。
- 系统缺少对自动标记结果、人工覆盖、规则版本和异常流水的统一管理能力。

## 3. Goals

### P0

- 将现有Excel规则纳入系统管理并支持上传、校验、预览和发布。
- 根据规则对新增流水或指定历史流水执行科目标记。
- 支持人工标记、飞书审批匹配和渠道规则匹配三类来源。
- 按统一优先级确定最终科目，并完整展示匹配依据。
- 支持未匹配、规则冲突和数据异常流水的人工处理。
- 支持人工修改和锁定，保证自动结果不能覆盖人工结果。
- 提供规则版本、匹配结果和人工操作的追溯信息。

### P1

- 提供自动匹配覆盖率、异常率、人工修改率等统计数据。
- 支持历史流水重新匹配前的影响预览。
- 支持规则版本停用和回滚的交互演示。

### P2

- 根据人工修改结果形成规则优化建议。
- 提供更丰富的平台和科目维度分析。

## 4. Non-Goals

- 第一阶段不建设可视化规则新增或编辑器。
- 不自动生成会计凭证、借贷分录或税务处理。
- 不使用AI、相似度、错别字纠正等模糊语义判断。
- 不自动把人工修改转化为正式规则。
- 不修改或覆盖原始流水字段。
- 原型阶段不接入真实后端、飞书开放平台或生产数据库。

## 5. Users and Use Cases

### Primary Users

- 资金运营：上传规则、查看匹配覆盖情况、发起历史重匹配。
- 财务人员：查看科目标记、处理未匹配和冲突流水、人工修改科目。
- 财务管理员：发布和回滚规则版本、查看操作记录。
- 产品及研发人员：通过原型确认页面结构、状态和业务规则。

### Key Use Cases

- 作为资金运营，我希望上传业务方维护的Excel规则，以便系统自动执行流水匹配。
- 作为财务人员，我希望快速查看未匹配和冲突流水，并完成科目标记。
- 作为财务人员，我希望看到每条流水的标记来源和匹配依据，以便判断结果是否可信。
- 作为财务管理员，我希望人工结果不会被自动匹配覆盖。
- 作为财务管理员，我希望查看规则版本及影响范围，以便安全发布或回滚规则。

## 6. User Journey

### 6.1 规则发布流程

1. 用户进入“规则管理”页面。
2. 用户上传Excel规则文件。
3. 系统解析第一张工作表并校验列名、必填项、字段值、重复和冲突。
4. 系统展示有效规则、错误规则、警告规则及版本变化预览。
5. 用户确认后发布有效规则。
6. 系统生成新版本，并将其设为当前生效版本。

### 6.2 流水自动标记流程

1. 新流水进入系统，或者用户发起历史流水重新匹配。
2. 系统先判断是否存在人工标记；若存在，直接使用人工结果。
3. 若不存在人工标记，判断是否存在有效飞书付款审批匹配结果。
4. 若没有飞书结果，读取当前生效的渠道规则执行匹配。
5. 唯一命中时写入科目；未命中、冲突或数据异常时进入异常队列。
6. 系统记录标记来源、匹配依据、规则版本和处理时间。

### 6.3 人工处理流程

1. 用户进入“异常待处理”或“流水列表”。
2. 用户打开流水详情，查看原始字段、各来源候选结果和处理记录。
3. 用户点击“人工标记”，选择一级、二级、三级科目并填写修改原因。
4. 系统将来源改为“人工标记”，默认锁定该流水。
5. 后续飞书审批或渠道规则不得覆盖人工结果。
6. 用户解除人工标记后，系统重新按照飞书优先、渠道规则其次的顺序计算结果。

## 7. Requirements

### 7.1 标记来源与优先级

| 优先级 | 来源 | 处理规则 |
| --- | --- | --- |
| 1 | 人工标记 | 最高优先级，可随时修改并默认锁定，其他来源不能覆盖 |
| 2 | 飞书付款审批 | 无人工标记时生效，可以覆盖渠道规则结果 |
| 3 | 渠道流水规则 | 无人工标记和有效飞书结果时生效 |
| 4 | 无结果 | 进入未匹配、冲突或数据异常状态 |

优先级与结果产生时间无关。系统应分别保存三类来源的结果，再计算最终生效科目，不应在覆盖时删除低优先级来源结果。

### 7.2 Excel规则结构

原型必须使用附件《科目匹配规则-原始配置.xlsx》作为规则数据源，兼容以下七列：

| Excel列 | 是否必填 | 说明 |
| --- | --- | --- |
| 平台 | 是 | Payoneer、PayPal、Worldfirst、PingPong等 |
| 账号 | 是 | 具体账号或“所有账户” |
| 检索字段 | 是 | 交易描述、备注、业务类型、code 类型等 |
| 检索关键词 | 是 | 用于执行匹配的具体值，长数字必须按字符串处理 |
| 一级科目 | 是 | 匹配结果 |
| 二级科目 | 是 | 匹配结果 |
| 三级科目 | 否 | 没有三级科目时允许为空 |

当前附件包含78条规则，涉及Payoneer、Airwallex、DAHSING、HSBC、Paypal、PingPong和Worldfirst七个平台。原型应解析全部规则，不应只硬编码少量示例。

当前Excel第42—46行缺少“检索字段”，应在导入预览中展示为5条无效规则，不能直接参与匹配。其余有效规则可进入发布预览。

### 7.3 默认匹配方式

- `code 类型`、`业务类型`、`交易类型`、`收款人姓名`、`电商平台/支付网关`：标准化后完全匹配。
- `交易描述`、`备注`：标准化后包含匹配。
- 标准化包括：去除首尾空格、英文统一为小写、连续空格合并为一个空格。
- 不删除数字、标点和特殊字符。
- 23位等长数字关键词必须保留为字符串，不允许转换成科学计数法或丢失精度。

### 7.4 渠道规则内部优先级

当一条流水命中多条渠道规则时，按以下顺序处理：

1. 具体账号优先于“所有账户”。
2. 完全匹配优先于包含匹配。
3. 更长关键词优先于更短关键词。
4. 显式规则优先级高的优先。
5. 多条最高优先级规则指向相同科目时，允许正常标记。
6. 多条最高优先级规则指向不同科目时，标记为“规则冲突”，不得静默选择。

原型需要演示以下真实规则风险：

- PingPong同一账号下，“入账”与“实时换汇转账-入账”存在包含关系，应由更长关键词优先。
- Worldfirst不同账号下，`Fund collection`分别对应资金转账收款和付款，账号必须参与匹配。
- PayPal的关键词`T`只有一个字符，如果采用包含匹配风险较高；在`code 类型`字段中必须按完全匹配处理。

### 7.5 功能需求

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-001 | P0 | 提供Excel规则上传入口 | 上传xlsx后能够进入解析和校验步骤 |
| FR-002 | P0 | 校验规则必填项及字段合法性 | 第42—46行显示为检索字段缺失，不进入有效规则 |
| FR-003 | P0 | 展示规则错误和警告 | 能筛选有效、错误、警告并查看具体原因 |
| FR-004 | P0 | 发布规则版本 | 确认发布后生成版本号并展示为当前生效版本 |
| FR-005 | P0 | 展示只读规则列表 | 能按平台、账号、字段、关键词和科目筛选全部规则 |
| FR-006 | P0 | 对流水执行渠道规则匹配 | 唯一命中时写入一级、二级、三级科目及规则依据 |
| FR-007 | P0 | 处理三类来源优先级 | 人工结果覆盖飞书，飞书覆盖渠道规则 |
| FR-008 | P0 | 展示流水列表 | 支持按日期、平台、账号、状态、来源和科目筛选 |
| FR-009 | P0 | 展示流水详情 | 展示原始字段、最终科目、三类来源结果和操作时间线 |
| FR-010 | P0 | 支持单笔人工标记 | 用户选择科目、填写原因并确认后立即成为最终结果 |
| FR-011 | P0 | 支持人工锁定 | 重新匹配不得覆盖人工锁定结果 |
| FR-012 | P0 | 提供异常待处理页面 | 分别展示未匹配、规则冲突和数据异常流水 |
| FR-013 | P1 | 支持历史重新匹配 | 可选择平台、账号和日期范围并预览影响结果 |
| FR-014 | P1 | 展示数据概览 | 展示匹配覆盖率、来源分布、异常量和平台分布 |
| FR-015 | P1 | 支持版本查看和回滚演示 | 可查看历史版本差异并执行确认回滚交互 |
| FR-016 | P0 | 保留审计信息 | 每次标记变化可查看来源、规则、操作人、时间和原因 |

### 7.6 非功能需求

| ID | 类别 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| NFR-001 | 一致性 | 重复执行同一规则和同一流水应产生相同结果 | 同一数据重复处理不会生成重复记录或不同科目 |
| NFR-002 | 可追溯 | 每个最终结果必须有来源 | 流水详情中始终能看到来源和依据 |
| NFR-003 | 安全性 | 自动任务不得覆盖人工锁定结果 | 所有重匹配场景均保留人工结果 |
| NFR-004 | 性能 | 原型中的列表和筛选交互保持流畅 | 500条模拟流水下筛选响应无明显卡顿 |
| NFR-005 | 可用性 | 关键状态使用文字和颜色双重表达 | 不仅依赖颜色区分匹配状态 |
| NFR-006 | 响应式 | 主要面向桌面后台 | 1280px以上完整展示，较窄窗口允许表格横向滚动 |

### 7.7 Acceptance Scenarios

- Given 一条流水同时存在人工、飞书和渠道规则结果，when 系统计算最终科目，then 采用人工科目，并展示人工锁定状态。
- Given 一条流水没有人工标记但存在有效飞书审批结果，when 系统计算最终科目，then 采用飞书审批科目并保留渠道候选结果。
- Given 一条流水只命中唯一渠道规则，when 匹配完成，then 写入对应科目、规则ID、规则版本、字段和关键词。
- Given 一条流水命中同优先级但不同科目的规则，when 匹配完成，then 状态为规则冲突且不写入最终科目。
- Given 用户上传当前Excel，when 系统完成校验，then 展示78条总规则，并将第42—46行识别为5条无效规则。
- Given 流水已被人工锁定，when 用户发起历史重新匹配，then 系统跳过该流水并在预览及结果中展示跳过数量。

## 8. UX and Content Notes

### 8.1 整体信息架构

左侧导航：

1. 数据概览
2. 流水列表
3. 异常待处理
4. 规则管理

规则版本详情、流水详情和上传步骤作为页面、抽屉或弹窗进入，不增加一级导航。

### 8.2 页面一：数据概览

页面目标：让管理人员快速了解科目标记的覆盖效果及待处理风险。

页面内容：

- 顶部标题、统计周期、最后更新时间。
- KPI卡片：流水总数、已标记数、自动覆盖率、未匹配数、冲突数、人工修改率。
- 标记来源分布：人工、飞书审批、渠道规则、无结果。
- 各平台匹配情况：按Payoneer、PayPal、Worldfirst、PingPong等展示覆盖率。
- 近7日匹配趋势。
- 待处理提醒，点击后进入异常列表。

### 8.3 页面二：流水列表

筛选区：

- 日期范围
- 平台
- 账号
- 收支方向
- 匹配状态
- 标记来源
- 一级、二级、三级科目
- 流水号或关键词搜索

列表字段：

- 流水时间
- 流水号
- 平台及账号
- 交易描述或主要检索内容
- 收支方向
- 金额及币种
- 一级、二级、三级科目
- 匹配状态
- 标记来源
- 操作

列表操作：

- 查看详情
- 人工标记或修改
- 解除人工锁定
- 选择多条流水发起重新匹配

### 8.4 流水详情抽屉

分为四个区块：

1. 原始流水：展示平台返回的全部匹配相关字段。
2. 最终科目标记：展示当前科目、来源、状态和更新时间。
3. 来源判断：分别展示人工、飞书审批和渠道规则候选结果，并标识最终采用项。
4. 操作记录：使用时间线展示首次标记、自动覆盖、人工修改和解除锁定。

### 8.5 人工标记弹窗

- 展示流水号、平台、金额和当前科目。
- 提供一级、二级、三级科目级联选择。
- 修改原因必填。
- “锁定人工结果”默认勾选。
- 明确提示：人工标记后，飞书审批及渠道规则不会覆盖该结果。

### 8.6 页面三：异常待处理

顶部使用标签页区分：

- 未匹配
- 规则冲突
- 数据异常

每条异常展示原因、关键原始字段、候选规则和推荐操作。规则冲突详情应并列展示不同候选规则及对应科目，不替用户自动选择。

### 8.7 页面四：规则管理

顶部区域：

- 当前生效版本
- 发布时间及发布人
- 有效规则数
- 覆盖平台数
- 上传Excel按钮
- 查看历史版本按钮

只读规则列表：

- 展示Excel七列，以及系统补充的规则ID、匹配方式、校验状态和版本。
- 支持平台、账号、检索字段、关键词、科目及状态筛选。
- 不提供逐条新增、修改和删除按钮。

### 8.8 Excel上传向导

步骤一：上传文件

- 拖拽或选择xlsx文件。
- 提供模板下载。
- 显示文件名、大小和上传时间。

步骤二：解析与校验

- 显示总规则数、有效数、错误数和警告数。
- 对当前附件展示：78条总规则、5条无效规则。
- 支持按状态筛选并查看行号及原因。

步骤三：影响预览

- 显示新增、修改、停用规则数。
- 展示可能改变科目的历史流水数量。
- 提示不会自动覆盖人工锁定结果。

步骤四：确认发布

- 填写版本说明。
- 确认发布有效规则。
- 发布成功后展示版本号和生效时间。

### 8.9 状态视觉规范

- 自动匹配：蓝色标签。
- 飞书审批：青绿色标签。
- 人工标记：紫色标签，并显示锁图标。
- 未匹配：灰色标签。
- 规则冲突：橙色标签。
- 数据异常：红色标签。
- 金额收入使用绿色正向标识，支出使用深色或红色负向标识，但不能仅通过颜色表达。

## 9. Data, Integrations, and Dependencies

### 9.1 原型技术假设

- 使用React、TypeScript和现代前端路由实现。
- 使用浏览器本地状态或localStorage模拟数据持久化。
- 使用SheetJS/xlsx在浏览器中解析Excel。
- 使用模拟数据代替真实后端、飞书API和资金流水接口。
- 所有关键按钮需要有真实可点击交互，而不是静态截图。

### 9.2 核心数据对象

- Transaction：流水原始信息。
- Rule：Excel规则及系统补充属性。
- RuleVersion：规则版本与发布记录。
- FeishuApprovalResult：飞书审批匹配结果。
- ManualMark：人工科目、原因和锁定状态。
- MatchResult：各来源候选结果及最终结果。
- AuditLog：科目变化及操作记录。

### 9.3 流水字段映射

原型流水对象至少包含：

- transactionDescription：交易描述
- note：备注
- businessType：业务类型
- codeType：code 类型
- payeeName：收款人姓名
- paymentGateway：电商平台/支付网关
- transactionType：交易类型

匹配时应根据Excel“检索字段”读取对应属性。

## 10. Analytics and Success Metrics

- 自动匹配覆盖率：自动匹配流水数 ÷ 总流水数。
- 已标记率：存在最终科目的流水数 ÷ 总流水数。
- 未匹配率、冲突率、数据异常率。
- 人工修改率：人工修改流水数 ÷ 已自动标记流水数。
- 按金额计算的人工修改率。
- 财务人员平均处理时间。
- 规则版本发布前后覆盖率变化。

原型使用模拟数据展示上述指标，不需要真实埋点。

## 11. Release Plan

### MVP原型

- 完成全部核心页面和主要交互。
- 使用真实Excel规则和模拟流水数据。
- 支持规则解析、校验、发布、匹配、人工覆盖和审计演示。

### 试运行

- 使用最近1—3个月历史流水进行影子匹配。
- 抽样验证各平台准确率及高金额流水。

### 正式上线

- 先选取结构化字段较完整的平台灰度开放。
- 验证后逐步扩展到全部配置平台。

### 回滚机制

- 保留上一规则版本。
- 支持停用当前版本并恢复上一版本。
- 人工标记和操作日志不随规则回滚删除。

## 12. Risks and Mitigations

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| 宽泛关键词误匹配 | 错误标记科目 | 明确匹配方式、长关键词优先、冲突不自动标记 |
| 同一描述在不同账号含义不同 | 收付款方向错误 | 平台和账号作为强制过滤条件 |
| Excel规则字段缺失 | 规则无法执行 | 上传阶段阻断无效规则并展示行号 |
| 人工结果被自动覆盖 | 财务确认结果失效 | 人工标记最高优先级并默认锁定 |
| 规则版本改变历史结果 | 难以追溯 | 重新匹配前预览影响并保留版本记录 |
| 长数字关键词精度丢失 | 规则永远无法命中 | 全链路按字符串解析和展示 |

## 13. Open Questions

- [x] 飞书付款审批单与流水的具体关联字段是什么：流水号、支付单号还是其他外部单号？
  - 结论：按**流水号**关联。
- [x] 飞书审批被撤回或作废后，是否需要实时回退到渠道规则结果？
  - 结论：审批不会失效。匹配错误时使用人工标记修正。
- [x] 第一批历史流水重新匹配的时间范围和平台范围是什么？
  - 结论：本期暂不考虑历史数据匹配，新规则只对后续新流水生效。
- [x] 科目字典是否由现有系统提供，还是原型暂用Excel中出现的科目生成？
  - 结论：原型从 Excel 有效科目去重，并补充飞书审批科目。
- [x] 正式系统中规则发布是否需要上传人与发布人分离？
  - 结论：不需要分离，同一财务管理员可完成上传和发布。

## 14. AI / Engineering Handoff

### Suggested Milestones

1. 搭建应用框架、导航、主题及模拟数据层。
2. 建立Excel规则解析、校验、版本和匹配引擎。
3. 完成数据概览、流水列表和详情交互。
4. 完成异常处理和人工标记流程。
5. 完成规则管理及上传发布向导。
6. 补齐审计、历史重匹配、空状态和错误状态。
7. 执行完整交互和视觉验收。

### Implementation Notes

- 原型优先使用React + TypeScript + Vite、Tailwind CSS、shadcn/ui、Lucide图标、Recharts、SheetJS和TanStack Table。
- 使用localStorage保存当前规则版本、人工修改和模拟发布结果。
- 将匹配引擎实现为独立纯函数，便于使用固定样例验证优先级和冲突逻辑。
- 原型中不得只展示静态页面；上传、筛选、抽屉、弹窗、发布、人工标记、解除锁定和重匹配必须可操作。
- 将附件Excel放在`public/templates/科目匹配规则-原始配置.xlsx`，同时允许用户上传其他同结构文件。

### QA Scenarios

- 人工标记同时存在飞书和渠道结果时，最终来源仍为人工。
- 飞书结果后到达时，覆盖已有渠道规则结果。
- 解除人工锁定后，回退到飞书结果；没有飞书时回退到渠道结果。
- Payoneer的`Transfer between balances`命中资金转账-货币兑换。
- HSBC的`CREDIT INTEREST`命中公司收入-利息收入。
- PingPong的`实时换汇转账-入账`不会错误采用较短的`入账`规则。
- Worldfirst的`Fund collection`根据具体账号产生不同结果。
- 第42—46行规则被识别为无效规则。
- 23位数字关键词按完整字符串展示和匹配。
- 多条最高优先级规则指向不同科目时进入规则冲突。

## 15. Changelog

- v0.1：整理背景、三类标记来源及渠道规则匹配思路。
- v0.2：补充UI页面、交互、Excel规则、数据模型及Cursor原型交接要求。
- v0.3：确认飞书按流水号关联；审批不失效、错误用人工标记修正；本期不做历史重匹配；上传与发布不分离。
