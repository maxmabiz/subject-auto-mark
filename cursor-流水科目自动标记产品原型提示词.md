# Cursor提示词：生成流水科目自动标记完整产品原型

请在当前项目中实现一个可运行、可交互的中文资金后台产品原型，产品名称为“流水科目自动标记”。不要只生成静态页面或线框图，要完成主要业务交互、状态变化、Excel解析和本地模拟数据闭环。

开始前请先读取并遵循项目中的以下材料：

1. `prd-流水科目自动标记产品原型.md`
2. `科目匹配规则-原始配置.xlsx`

如果当前项目为空，请直接创建前端项目；如果已有项目，请先理解现有技术栈和目录结构，在不破坏已有功能的前提下实现。

## 一、产品目标

当前财务人员需要在Excel中逐笔为资金流水标记科目，再将结果导入系统。现在需要把业务方维护的Excel规则交给系统执行，自动完成大部分流水的科目标记，只把未匹配、规则冲突和数据异常的流水交给财务人工处理。

系统存在三类科目标记来源，最终结果必须遵循固定优先级：

1. 人工标记：最高优先级
2. 飞书付款审批单匹配：中优先级
3. 渠道流水规则识别：最低优先级

优先级与结果产生时间无关。人工标记一旦锁定，飞书和渠道规则都不能覆盖；解除人工锁定后，重新按飞书优先、渠道规则其次进行计算。

本项目只做科目标记，不生成会计凭证。

## 二、技术实现要求

优先采用以下技术栈：

- React
- TypeScript
- Vite
- Tailwind CSS
- shadcn/ui或同等质量的无障碍组件库
- Lucide React图标
- Recharts图表
- TanStack Table表格
- SheetJS/xlsx解析Excel
- React Router管理路由
- localStorage模拟持久化

如果项目已有等价技术栈，可沿用现有方案，不要为了完全一致而重建项目。

完成后必须支持：

```bash
npm install
npm run dev
npm run build
```

不得依赖真实后端、数据库或飞书API。使用前端模拟数据和localStorage完成演示。匹配引擎必须写成独立、可测试的纯函数。

## 三、视觉与体验要求

设计为专业、克制、清晰的资金管理后台：

- 中文界面，默认浅色主题。
- 主要适配1440×900桌面端，1280px以上可完整使用。
- 左侧固定导航约220px，右侧为内容区。
- 主色使用稳重的蓝色或靛蓝色。
- 卡片、表格、抽屉和弹窗保持统一间距与圆角。
- 金额、科目和匹配状态要有清晰视觉层级。
- 状态必须同时使用文字和颜色，不能只依靠颜色。
- 页面需要具备加载、空数据、错误、成功和确认状态。
- 所有关键按钮必须真实可点击并产生可见结果。
- 不要使用过度渐变、3D效果、巨大圆角或花哨装饰。

状态颜色建议：

- 自动匹配：蓝色
- 飞书审批：青绿色
- 人工标记：紫色，并显示锁定图标
- 未匹配：灰色
- 规则冲突：橙色
- 数据异常：红色

## 四、信息架构

左侧一级导航包含：

1. 数据概览
2. 流水列表
3. 异常待处理
4. 规则管理

顶部区域展示产品名称、当前规则版本、数据更新时间和当前用户“财务管理员”。

规则版本详情、流水详情、人工标记、历史重匹配和Excel上传作为二级页面、右侧抽屉或弹窗实现。

## 五、页面一：数据概览

实现一个可交互的数据看板，包含：

### KPI卡片

- 流水总数
- 已标记流水数
- 自动匹配覆盖率
- 未匹配数
- 规则冲突数
- 人工修改率

使用一组合理的模拟数据，例如：

- 流水总数：12,486
- 已标记：11,324
- 自动覆盖率：88.2%
- 未匹配：912
- 规则冲突：250
- 人工修改率：1.8%

### 图表

- 标记来源分布：人工、飞书审批、渠道规则、无结果
- 各平台匹配覆盖率：Payoneer、PayPal、Worldfirst、PingPong、Airwallex、DAHSING、HSBC
- 最近7日流水匹配趋势

### 待处理提醒

展示未匹配、规则冲突和数据异常数量。点击任一项后跳转到“异常待处理”页面并自动选择对应标签页。

## 六、页面二：流水列表

### 筛选区

提供以下筛选条件：

- 日期范围
- 平台
- 账号
- 收支方向
- 匹配状态
- 标记来源
- 一级科目
- 二级科目
- 三级科目
- 流水号或关键词搜索

筛选条件应真正作用于前端模拟数据。提供“重置筛选”和“查询”交互。

### 表格字段

- 流水时间
- 流水号
- 平台
- 账号
- 交易描述或主要匹配字段
- 收支方向
- 金额
- 币种
- 一级科目
- 二级科目
- 三级科目
- 匹配状态
- 标记来源
- 操作

支持分页、列内容省略提示、表格横向滚动和行选择。

### 操作

- 点击“查看”打开流水详情抽屉。
- 点击“人工标记”打开科目修改弹窗。
- 人工锁定流水显示“解除锁定”。
- 选中多条流水后可以点击“重新匹配”。
- 重新匹配前展示影响预览，人工锁定流水必须排除。

## 七、流水详情抽屉

详情抽屉建议宽度为640—720px，包含四个区域：

### 1. 原始流水信息

展示：

- 流水号
- 平台
- 账号
- 流水时间
- 收支方向
- 金额和币种
- 交易描述
- 备注
- 业务类型
- code 类型
- 收款人姓名
- 电商平台/支付网关
- 交易类型
- 飞书审批单号

没有值的字段显示“-”。

### 2. 当前最终科目

展示一级、二级、三级科目、标记来源、状态、更新时间和是否人工锁定。

### 3. 来源判断

使用三个并列卡片或纵向步骤展示：

- 人工标记候选结果
- 飞书审批候选结果
- 渠道规则候选结果

标识最终采用了哪个来源，并解释原因。例如：“存在人工锁定，因此未采用飞书及渠道规则结果”。

### 4. 操作记录

使用时间线展示：

- 流水进入系统
- 渠道规则自动命中
- 后续关联飞书审批并覆盖
- 财务人工修改并锁定
- 解除人工锁定并回退

每条记录显示时间、操作人或系统、变更前后科目和原因。

## 八、人工标记弹窗

弹窗中展示流水摘要和当前科目，并提供：

- 一级科目选择
- 二级科目选择
- 三级科目选择，可为空
- 修改原因，必填
- “锁定人工结果”开关，默认开启

科目使用级联选择，二级选项跟随一级科目变化，三级选项跟随二级变化。

点击确认后：

- 更新最终科目。
- 来源改为“人工标记”。
- 状态改为“人工标记”或“人工锁定”。
- 记录人工修改日志。
- 流水列表、详情和概览数据同步刷新。
- localStorage保存结果，页面刷新后仍然存在。

解除人工锁定时需要二次确认。解除后重新计算：有飞书结果则采用飞书，没有飞书则采用渠道规则结果。

## 九、页面三：异常待处理

顶部包含三个标签页：

1. 未匹配
2. 规则冲突
3. 数据异常

每个标签显示数量。

### 未匹配

展示没有任何规则命中的流水及主要检索字段。支持人工标记。

### 规则冲突

展示同时命中的候选规则，并列显示：

- 规则ID
- 平台和账号范围
- 检索字段
- 关键词
- 匹配方式
- 对应科目
- 优先级

不得自动替用户选择不同科目的冲突规则。用户可以人工指定最终科目。

### 数据异常

展示缺少平台、账号、规则要求字段或字段格式异常的流水，并明确错误原因。

## 十、页面四：规则管理

第一期不开发可视化规则编辑器。页面只提供规则查看、Excel上传、版本发布和版本追溯。

### 顶部版本卡片

展示：

- 当前版本，例如V1.0.0
- 发布状态
- 发布时间
- 发布人
- 有效规则数
- 覆盖平台数
- “上传Excel”按钮
- “查看历史版本”按钮

### 只读规则表格

展示Excel七列：

1. 平台
2. 账号
3. 检索字段
4. 检索关键词
5. 一级科目
6. 二级科目
7. 三级科目

另外展示系统字段：规则ID、匹配方式、校验状态、规则版本。

支持按平台、账号、检索字段、关键词、科目和校验状态筛选，但不得提供逐条新增、编辑或删除按钮。

## 十一、Excel规则导入

将提供的`科目匹配规则-原始配置.xlsx`放到：

`public/templates/科目匹配规则-原始配置.xlsx`

页面同时提供模板下载和本地xlsx上传能力。

必须解析Excel第一张工作表，并识别以下列名：

- 平台
- 账号
- 检索字段
- 检索关键词
- 一级科目
- 二级科目
- 三级科目

不要只硬编码几条规则。提供的Excel包含78条真实规则，原型需要能读取并展示全部规则。

### 数据解析要求

- 所有关键词先按字符串读取。
- 23位数字关键词不得转成科学计数法，不得丢失精度。
- 空单元格转换为`null`或空字符串。
- 保留原始Excel行号，错误提示需要显示行号。

### 上传向导

#### 步骤1：上传文件

- 支持拖拽和文件选择。
- 仅接受xlsx。
- 展示文件名、大小和上传时间。

#### 步骤2：解析与校验

展示：

- 总规则数
- 有效规则数
- 错误数
- 警告数

当前文件应显示78条总规则。第42—46行因为“检索字段”为空，应被识别为5条无效规则并展示具体原因。

阻断性错误：

- 平台为空
- 账号为空
- 检索字段为空
- 关键词为空
- 一级或二级科目为空
- 检索字段不在支持字段列表中
- 同一作用范围、同一字段、同一关键词配置不同科目

警告：

- 一个关键词包含另一个关键词
- 关键词仅有一个字符
- “所有账户”规则与具体账号规则重叠
- 同一关键词在不同账号对应不同科目

#### 步骤3：影响预览

展示模拟的新增、修改、停用规则数，以及预计影响的历史流水数。明确提示人工锁定结果不会被覆盖。

#### 步骤4：确认发布

- 输入版本说明。
- 可以选择“仅发布有效规则”。
- 发布成功后生成新版本号。
- 更新当前生效版本和规则列表。
- 弹出成功提示。

## 十二、规则匹配引擎

创建独立模块，例如：

`src/domain/matching/`

至少包含：

- 字段映射
- 字符串标准化
- 匹配方式判断
- 候选规则筛选
- 候选优先级排序
- 冲突判断
- 三类来源的最终结果决策

### 流水字段映射

Excel检索字段映射到流水对象：

- 交易描述 → `transactionDescription`
- 备注 → `note`
- 业务类型 → `businessType`
- code 类型 → `codeType`
- 收款人姓名 → `payeeName`
- 电商平台/支付网关 → `paymentGateway`
- 交易类型 → `transactionType`

### 默认匹配方式

标准化操作：

- 去除首尾空格
- 英文转小写
- 连续空格合并为一个空格
- 保留数字、标点和特殊字符

匹配方式：

- `交易描述`、`备注`：包含匹配
- `code 类型`、`业务类型`、`交易类型`、`收款人姓名`、`电商平台/支付网关`：完全匹配

### 渠道规则优先级

1. 具体账号优先于“所有账户”
2. 完全匹配优先于包含匹配
3. 长关键词优先于短关键词
4. 显式优先级高的优先
5. 最高优先级候选指向相同科目时允许正常标记
6. 最高优先级候选指向不同科目时返回规则冲突

### 三类来源最终决策

请实现类似以下行为，但可以使用更合理的类型设计：

```ts
finalResult = manualResult?.locked
  ? manualResult
  : validFeishuResult
    ? validFeishuResult
    : uniqueRuleResult
      ? uniqueRuleResult
      : unresolvedResult;
```

低优先级候选结果仍需保存并在流水详情中展示。

## 十三、必须包含的模拟数据

生成不少于60条渠道流水，覆盖全部七个平台和以下场景：

1. 人工标记覆盖飞书和渠道规则。
2. 飞书审批覆盖渠道规则。
3. 渠道规则唯一命中。
4. 无规则命中。
5. 多规则冲突。
6. 流水缺少规则所需字段。
7. 人工锁定后重新匹配。
8. 解除人工锁定后回退到飞书或渠道规则。

必须使用附件中的真实规则构造代表性样例：

- Payoneer，交易描述`Transfer between balances` → 资金转账 / 资金转账-货币兑换
- Payoneer，交易描述`shopify` → 电商业务 / 电商业务-收款
- HSBC，交易描述`CREDIT INTEREST` → 公司收入 / 公司收入-利息收入
- PayPal，code 类型`准备金冻结` → 受限资金
- PingPong，业务类型`实时换汇转账-入账` → 资金转账 / 资金转账-货币兑换
- Worldfirst-muxue，交易描述`Fund collection` → 资金转账-收款
- Worldfirst-Besttech01，交易描述`Fund collection` → 资金转账-付款
- Worldfirst-muxue02，交易类型`CARD_REFUND` → 公司收入 / 公司收入-其他收入

不要擅自修改业务方Excel中现有科目映射，即使某些映射从会计角度看可能需要进一步确认。产品原型应忠实执行当前配置。

模拟飞书审批类型至少包含：

- 软件及信息服务费 → 公司费用 / 公司费用-信息化费用
- 工资奖金 → 公司费用 / 公司费用-职工薪酬 / 公司费用-职工薪酬-工资奖金
- 采购付款 → 履约业务 / 履约业务-采购付款
- 广告投放 → 广告业务 / 广告业务-付款

科目字典可以从Excel所有有效规则的一级、二级、三级科目去重生成，并补充飞书审批使用的科目。

## 十四、数据模型建议

至少定义以下TypeScript类型：

- `Transaction`
- `Rule`
- `RuleVersion`
- `SubjectPath`
- `FeishuApprovalResult`
- `ManualMark`
- `ChannelRuleResult`
- `FinalMatchResult`
- `AuditLog`

`Transaction`至少包括：

- id
- transactionNo
- platform
- account
- transactionTime
- amount
- currency
- direction
- transactionDescription
- note
- businessType
- codeType
- payeeName
- paymentGateway
- transactionType
- feishuApprovalId

`FinalMatchResult`至少包括：

- transactionId
- status
- source
- subject
- matchedRuleId
- ruleVersion
- matchedField
- matchedKeyword
- matchedRawValue
- updatedAt
- locked
- explanation

## 十五、历史重新匹配

在流水列表中支持选择日期范围、平台和账号发起重新匹配。

执行前展示预览弹窗：

- 预计处理流水数
- 可重新匹配数
- 被人工锁定而跳过的数量
- 新增标记数
- 科目可能变化数
- 预计未匹配数
- 预计冲突数

点击确认后使用前端模拟异步任务，显示进度和完成提示。

处理规则：

- 人工锁定流水永远跳过。
- 原自动匹配结果允许更新。
- 原飞书审批结果在审批仍有效时保持优先。
- 处理完成后更新列表、统计和操作记录。

## 十六、规则版本

提供版本历史抽屉或页面，展示：

- 版本号
- 状态
- 发布时间
- 发布人
- 版本说明
- 总规则数
- 有效规则数
- 错误规则数
- 与上一版本的新增、修改、停用数量

实现“查看详情”和“回滚”交互。回滚需要二次确认，并明确说明：

- 回滚只改变当前生效规则版本。
- 不删除历史匹配记录。
- 不覆盖人工标记。

## 十七、测试要求

至少为匹配引擎编写以下单元测试：

1. 忽略英文大小写。
2. 去除首尾空格并合并连续空格。
3. 交易描述使用包含匹配。
4. code 类型使用完全匹配。
5. 具体账号优先于所有账户。
6. 长关键词优先于短关键词。
7. 同优先级不同科目返回冲突。
8. 人工结果优先于飞书和渠道规则。
9. 飞书结果优先于渠道规则。
10. 人工锁定结果不会被重新匹配覆盖。
11. 解除人工锁定后正确回退。
12. 23位数字关键词完整保留并可以匹配。

同时完成以下手工验证：

- 四个一级导航页面均可正常访问。
- 页面刷新后人工标记和规则版本仍然存在。
- Excel上传向导四个步骤可以完整走通。
- 第42—46行显示为无效规则。
- 流水筛选、详情抽屉、人工标记和解除锁定可用。
- 异常标签页可以根据状态正确过滤。
- 重新匹配会跳过人工锁定结果。
- `npm run build`成功且无TypeScript错误。

## 十八、实施顺序

请按照以下顺序实施，不要一开始只堆页面：

1. 检查项目结构和附件文件。
2. 搭建React、TypeScript、路由、Tailwind和基础布局。
3. 定义数据模型和模拟数据。
4. 实现Excel解析、校验和规则匹配引擎，并先完成单元测试。
5. 实现数据概览。
6. 实现流水列表、详情和人工标记。
7. 实现异常待处理。
8. 实现规则管理和Excel上传发布向导。
9. 实现版本、历史重匹配和审计交互。
10. 补齐空状态、错误状态、响应式和视觉细节。
11. 运行测试和生产构建，修复错误。

## 十九、完成标准

只有满足以下条件才算完成：

- 项目可以直接安装依赖并启动。
- 四个一级页面完整实现。
- 关键抽屉、弹窗、筛选、上传、发布、人工标记和重匹配均可交互。
- 实际解析并展示提供的78条Excel规则。
- 正确识别第42—46行的5条无效规则。
- 匹配引擎遵循渠道规则优先级及三类来源总优先级。
- 人工锁定结果不会被自动覆盖。
- 页面视觉达到可用于产品评审的高保真程度。
- 核心匹配逻辑有自动化测试。
- `npm run build`成功。

完成后请输出：

1. 实现内容总结。
2. 主要目录和文件说明。
3. 启动及构建命令。
4. 已完成的测试。
5. 仍采用模拟数据或需要真实接口支持的部分。
6. 任何尚未解决的产品问题。
